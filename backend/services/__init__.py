# services package
# Key entry points:
#   prediction_service.PredictionService  — model load + predict dispatch
#   client_service.ClientService          — client CRUD + 360 profile
#   analytics_service.AnalyticsService    — dashboard KPIs
#   trainer.TrainingService               — ML training orchestration
#   audit_service.log_action              — audit logging
